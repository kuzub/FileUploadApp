<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:FileUploadApp.ViewModels"
             x:Class="FileUploadApp.MainPage"
             x:DataType="viewmodels:MainViewModel">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a hovercraft number nine" />

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <Button
                Text="Pick Images from Gallery"
                Command="{Binding PickImagesCommand}"
                SemanticProperties.Hint="Select multiple images from your gallery"
                HorizontalOptions="Fill" />

            <Label
                Text="{Binding SelectedImagesText}"
                IsVisible="{Binding HasSelectedImages}"
                HorizontalOptions="Center"
                FontAttributes="Bold" />

            <CollectionView
                ItemsSource="{Binding SelectedImagePaths}"
                SelectionMode="None"
                HeightRequest="300"
                IsVisible="{Binding HasSelectedImages}">
                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Vertical" Span="2" />
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="x:String">
                        <Frame Padding="5" Margin="5" CornerRadius="10">
                            <Image Source="{Binding .}" 
                                   Aspect="AspectFill"
                                   HeightRequest="150"
                                   WidthRequest="150" />
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <ActivityIndicator IsRunning="{Binding IsBusy}"
                             IsVisible="{Binding IsBusy}"
                             Color="{StaticResource Primary}"
                             HorizontalOptions="Center" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
